interface:
  display_name: "BDD Consistency Check"
  short_description: "Detect and resolve contradictions across BDD spec files"
  brand_color: "#15803D"
  default_prompt: "Use $consistency-check to validate BDD specs for conflicts."

policy:
  allow_implicit_invocation: true
